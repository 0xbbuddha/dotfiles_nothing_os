# PAM stack for the lock screen's fingerprint attempt, kept separate from
# the password one so a failed finger never counts against the password
# tries. `sufficient` and nothing else: if pam_fprintd refuses, the stack
# has no fallback module and the attempt simply fails.
auth    sufficient    pam_fprintd.so
