import QtQuick
import ".."

// Four, seven, eight. The long exhale is the whole point: it is what
// actually slows the heart, and why this one is for winding down.
WBreath {
    inhale: 4; hold: 7; exhale: 8; rest: 0
    title: "Relax"
}
